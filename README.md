# docker_compose-postgres-express
- Убедиться, что установлен Docker
- пропсать команду "docker-compose up -d"
    -- фронт - http://localhost:3000/
    -- сервер - http://localhost:3001/
- для остановки процесса "docker-compose down"
